<form method="post" action="{{ path('app_electrician_delete', {'id': electrician.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ electrician.id) }}">
    <button class="btn">Delete</button>
</form>

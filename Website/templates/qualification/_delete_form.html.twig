<form method="post" action="{{ path('app_qualification_delete', {'id': qualification.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ qualification.id) }}">
    <button class="btn">Delete</button>
</form>
